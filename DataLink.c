#include "DataLink.h"

INT GetStateEvents(HANDLE* hEvents, INT state) {
    
    switch (state) {
        case STATE_IDLE:
        case STATE_T1:
        case STATE_T3:
        case STATE_R2:
            break;
    }
}